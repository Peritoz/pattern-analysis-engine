module.exports = (id, identifier, name, maintype, isPatternNode) => {
    return {
        _id: id,
        identifier: identifier,
        name: name,
        maintype: maintype,
        ispatternnode: isPatternNode,
    }
};